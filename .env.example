# Upstash Redis (cole o URL e Token do dashboard do Upstash)
# https://console.upstash.com → seu DB → REST API
KV_REST_API_URL=https://your-db.upstash.io
KV_REST_API_TOKEN=AYx...

# Opcional: pasta onde data.json é armazenado em dev local (default: raiz do projeto)
# DATA_DIR=/data
